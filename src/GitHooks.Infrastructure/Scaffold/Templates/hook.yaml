# Generated by githooks tool v{{VERSION}}
# Hook: {{HOOK_NAME}}
# {{HOOK_DESCRIPTION}}
# Define the steps to run for this hook below.

steps:
  - name: example
    run: echo "Running {{HOOK_NAME}} hook"
